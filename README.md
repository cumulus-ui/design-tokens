# @cumulus-ui/design-tokens

CSS custom properties and JS token values from the Cloudscape Design System.

Cumulus UI components use design tokens (colors, spacing, typography, motion) as CSS custom properties. This package provides those tokens as a standalone dependency, making them available outside of React-based Cloudscape applications.

## Install

```bash
npm install @cumulus-ui/design-tokens
```
